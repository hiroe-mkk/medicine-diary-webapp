package example.domain.model.medicine.medicineImage

import example.domain.shared.type.*

/**
 * 薬画像URL
 */
class MedicineImageURL(endpoint: String,
                       path: String) : URL(endpoint, path)