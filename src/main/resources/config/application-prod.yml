spring:
  datasource:
    driverClassName: 'com.mysql.cj.jdbc.Driver'
    url: 'jdbc:mysql://${MYSQL_HOST}:3306/${MYSQL_DATABASE_NAME}'
    username: '${MYSQL_USERNAME}'
    password: '${MYSQL_PASSWORD}'

aws:
  s3:
    endpoint: '${AWS_S3_ROOT_PATH}'
    bucketName: '${AWS_S3_BUCKET}'